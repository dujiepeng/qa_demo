# 项目总结 - qa_flutter

## 项目概况
`qa_flutter` 是一个基于 Flutter 开发的即时通讯演示应用，主要用于演示、集成和测试 `im_flutter_sdk` (环信即时通讯 SDK)。它同时支持手机和平板（Pad）两种布局模式。

## 核心技术栈
- **框架**: Flutter (Dart SDK ^3.9.2)
- **主要依赖**:
    - `im_flutter_sdk`: ^4.17.0 (环信 IM SDK)
    - `provider`: ^6.1.1 (状态管理)
    - `intl`: ^0.20.2 (日期格式化)
    - `chat_uikit_theme`: 1.0.4 (UI 主题库)
    - `shared_preferences`: 配置持久化

## 项目目录结构
- **lib/common**: 应用程序公用部分。
    - `widgets/`: 通用 UI 组件。
    - `utils/`: 工具类，包括日志服务 (`LogService`)、版本管理 (`VersionManager`)、响应式布局工具等。
    - `pages/`: 跨端的通用页面（如设置页、联系人页、日志内容查看页等）。
- **lib/mobile**: 专门针对手机终端适配的 UI 页面。
- **lib/pad**: 专门针对平板终端适配的 UI 布局和控制器，近期进行了大规模重构，实现了侧边栏导航和局部页面切换。
- **lib/test_pages**: 各种专项功能的测试页面，包括单聊 (`single`)、群聊 (`group`)、聊天室 (`chatroom`)。
- **lib/uikit**: 包含环信预置的 UI 核心组件和逻辑，是应用 UI 的核心。
- **lib/config**: 存储应用的基础配置信息。
- **lib/theme**: 定义应用的全局主题色调和样式。

## 当前功能实现
### 基础架构
- **状态管理**: 使用 `Provider` 全局管理 `AppSettings`、`VersionManager` 和 `LogService`。
- **配置持久化**: `AppSettings` 持久化存储暗黑模式、登录状态、AppKey 及服务器配置。
- **日志系统**: 全局 `LogService`，支持跨页面实时记录操作日志，专为 QA 测试设计。

### 功能模块
- **用户认证**: 完整的登录/注销流程，支持登录状态保持。
- **Pad 端分屏体验**:
    - 全新的侧边栏导航轨 (`NavigationRail`)。
    - 支持顶部工具栏与详情页 AppBar 内容的自动同步。
    - 局部导航系统，支持在不丢失上下文的情况下切换 Tab。
    - **高度可自定义**: 操作日志区域现支持手动垂直拖动缩放，适配不同屏幕尺寸和调试需求。
- **测试专项**: 提供针对单聊、群聊、聊天室的详细功能测试界面。
- **版本管理**: 集成了基于 GitHub Release 的自动更新检查机制。

## 待改进点
- **功能填充**: “单聊”和“群聊”测试子页面的原子化功能点（如消息发送、附件处理）需不断同步 SDK 最新版本。
- **错误提示**: 针对 SDK 抛出的特定错误码，需提供更直观的中文提示。
- **UI 统一性**: 持续打磨手机端与 Pad 端在 UI 逻辑上的对等表现。
